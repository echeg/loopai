# first review prompt
# this prompt is used for the first (comprehensive) review pass in phase 2
# launches 5 parallel reviewer agents plus optional project-specific ones
#
# available variables:
#   {{PLAN_FILE}} - path to the plan file being executed
#   {{PROGRESS_FILE}} - path to the progress log (task execution + previous reviews)
#   {{GOAL}} - human-readable goal description
#   {{DEFAULT_BRANCH}} - default branch name (main, master, trunk, etc.)
#   {{BACKLOG_DIR}} - backlog directory for out-of-scope findings (default: docs/backlog)
#   {{agent:<name>}} - expands to the executor-appropriate agent invocation
#                      (Task tool for claude, spawn_agent for codex)
#   {{agents:<dynamic>}} - the literal placeholder, written without the angle
#                      brackets (see Step 2b below). this leading comment block is
#                      stripped before expansion, but a header trimmed down to a
#                      single "# ..." line is kept, and a literal placeholder left
#                      there would render the catalog twice. expands to a catalog of
#                      the project-specific agents: every agent file whose frontmatter
#                      carries a non-empty description. each catalog entry lists the
#                      agent name, its description, and a ready-to-use invocation
#                      snippet. renders "(no project-specific agents configured)"
#                      when the project defines none
#
# agents are defined in ~/.config/loopai/agents/ (user), .loopai/agents/ (project)
# or pkg/config/defaults/agents/ (builtin)

Code review of: {{GOAL}}

Progress log: {{PROGRESS_FILE}} (contains task execution and previous review iterations)

## Step 1: Get Branch Context

Run both commands to understand what was done:
- `git log {{DEFAULT_BRANCH}}..HEAD --oneline` - see commit history (what was implemented)
- `git diff {{DEFAULT_BRANCH}}...HEAD` - see actual code changes

## Step 2: Launch ALL 5 Review Agents IN PARALLEL

CRITICAL: All 5 agent invocations — plus any project-specific agents you select in Step 2b — MUST be issued in a single message for true parallel execution.
These agents are fully independent — no shared state, no dependencies between them, no ordering requirements.
Under claude executor: do NOT use run_in_background. Foreground Task tool calls in the same message run in parallel and block until all complete — no TaskOutput polling needed.
Under codex executor: do NOT serialize spawn_agent calls; emit them all in one response and then call wait_agent on the full set.

CRITICAL: Do NOT proceed to Step 3 until ALL launched agents — the 5 base agents and any Step 2b agents — have returned results.

IMPORTANT: Read Step 2b before issuing the message, so the base agents and the project-specific ones go out together.

CRITICAL: Do NOT embed code or diffs in agent prompts. Keep prompts short — the agent expansions below tell each agent to run git commands themselves. Embedding large diffs into agent prompts causes slow parallel launch and burns tokens.

{{agent:quality}}
{{agent:implementation}}
{{agent:testing}}
{{agent:simplification}}
{{agent:documentation}}

## Step 2b: Select Project-Specific Agents (typically 0-3)

This project may define extra narrow review agents covering problem classes the 5 base agents do not. Catalog:

{{agents:dynamic}}

Selection rules:
- pick only agents whose focus actually matches the changed files and the nature of the diff (from Step 1)
- typically 0-3 agents; selecting NONE is valid and common — do not stretch to find a match
- launch every selected agent in the SAME single message as the 5 base agents above, never as a follow-up message
- use the invocation snippet from the catalog as-is
- same rule as Step 2: do NOT embed code or diffs in agent prompts — the snippets tell each agent to fetch the diff itself
- their findings are handled exactly like base agent findings in Step 3: verified first, then fixed if confirmed

## Step 3: Collect, Verify, and Fix Findings

After agents complete:

### 3.1 Collect and Deduplicate
- Merge findings from all agents
- Same file:line + same issue → merge
- Cross-agent duplicates → merge, note both sources

### 3.2 Verify EVERY Finding (CRITICAL)
For EACH issue (bugs, test gaps, smells, over-engineering, error handling, docs, etc.):
1. Read actual code at file:line
2. Check full context (20-30 lines around)
3. Verify issue is real, not a false positive
4. Check for existing mitigations

Classify as:
- CONFIRMED: Real issue, fix it
- FALSE POSITIVE: Doesn't exist or already mitigated - discard
- OUT OF SCOPE: Real issue, but outside this plan's scope - do NOT fix it, file it to the backlog

Out-of-scope findings go to the backlog instead of into this branch:
- create {{BACKLOG_DIR}}/<kebab-slug>.md: a "# <short problem title>" heading, then "- found: <YYYY-MM-DD>, plan: <plan-name>, phase: internal review",
  "- severity: minor|major", "- area: <primary file or package>", then a short description with file:line references and a suggested fix direction
- list existing files in {{BACKLOG_DIR}}/ first - if a similar entry is already there, update it instead of creating a duplicate
- a pre-existing linter error or failing test is never out of scope - fix it, do not file it
- never file a finding about code this branch changed: a defect in the branch's own work is in scope whether or
  not {{PLAN_FILE}} names a plan, and with no plan the branch diff is the whole scope
- stage the new entry with `git add <path>`: it is untracked, so no commit picks it up on its own, and the entry
  would die with the worktree
- commit the entry with your fixes, or on its own as `git commit -m "docs: add backlog entry" -- <entry>` when
  there is nothing else to commit - the pathspec keeps anything the user had already staged out of that commit
- filing an entry is not a fix: it does not count as a confirmed issue in the Step 4 signal logic

IMPORTANT: Pre-existing issues (linter errors, failed tests) should also be fixed.
Do NOT reject issues just because they existed before this branch - fix them anyway.

### 3.3 Fix All Confirmed Issues
1. Fix all CONFIRMED issues (all types: bugs, tests, smells, docs, etc.)
2. Run tests and linter to verify fixes - ALL tests must pass, ALL linter issues resolved
3. Stage every file you created or modified, including any backlog entry, with `git add <paths>`, then commit:
   `git commit -m "fix: address code review findings"`. Do NOT `git add -A`: `--review`, `--external-only`,
   and `--codex-only` run in the user's own checkout with no worktree, so a sweep commits their unrelated work.

## Step 4: Signal Completion

SIGNAL LOGIC - READ CAREFULLY:

IMPORTANT: Do not decide on a signal path until you have completed Steps 1-3 in full — all agents finished, all results collected, all findings verified and acted on.

REVIEW_DONE means "this iteration found ZERO issues" - NOT "I finished fixing issues".

Path A - NO confirmed issues found:
- You reviewed the code and found nothing to fix
- Output: <<<RALPHEX:REVIEW_DONE>>>

Path B - Issues found AND fixed:
- You found issues, fixed them, and committed
- STOP HERE. Do NOT output any signal. Do NOT output REVIEW_DONE.
- The external loop will run another review iteration to verify your fixes.
- Your fixes might have introduced new issues - another iteration must check.

Path C - Issues found but cannot fix:
- Output: <<<RALPHEX:TASK_FAILED>>>

OUTPUT FORMAT: No markdown formatting (no **bold**, `code`, # headers). Plain text and - lists are fine.
