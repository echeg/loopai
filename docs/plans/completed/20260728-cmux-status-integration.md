# cmux status integration

## Overview

Показывать работу ralphex в сайдбаре cmux (macOS-терминал на базе Ghostty): спиннер на время прогона,
пилюля с текущей фазой, прогресс-бар по задачам плана, уведомления когда нужен ввод пользователя
и по завершении прогона.

Проблема: при параллельном запуске нескольких ralphex в разных воркспейсах cmux не видно, какой из них
что делает — cmux детектит только агентов с установленными хуками (claude, codex, pi и т.д.), а ralphex
для него невидим. Свои короткие `claude -p` сессии ralphex порождает десятками за прогон, поэтому даже
там, где cmux их замечает, статус мигает и не несёт смысла.

Решение целиком на стороне ralphex: вызовы публичного CLI `cmux`, никаких правок в cmux и никаких
зависимостей на его приватный сокет-протокол. Вне cmux (нет бинаря в PATH или нет `CMUX_WORKSPACE_ID`)
всё превращается в no-op с нулевой стоимостью.

Ключевые свойства:

- **Best-effort.** Индикация не влияет на ход прогона. Любая ошибка вызова `cmux` игнорируется, прогон
  продолжается. Это индикация, а не функциональность.
- **Никаких правок в `pkg/processor`.** События берутся из уже существующих точек: `status.PhaseHolder`
  для фаз, plan-файл для прогресса, `InputCollector` для вопросов.
- **Гарантированная очистка.** `workspace loading` на стороне cmux не имеет TTL: если ralphex умер, не
  сняв спиннер, тот висит в сайдбаре до перезапуска cmux.

## Context (from discovery)

Изучен исходник cmux (`.reference/manaflow-ai-cmux`, shallow clone). Существенное:

**Поверхность интеграции (публичный CLI, правок cmux не требует):**

| Команда | Эффект в cmux |
|---|---|
| `cmux workspace loading on\|off --id ralphex` | спиннер в сайдбаре + лейн воркспейса → `working` |
| `cmux set-status <key> <value> --icon --color --priority` | пилюля-статус в строке таба |
| `cmux clear-status <key>` | снять пилюлю |
| `cmux set-progress <0.0-1.0> --label <text>` | прогресс-бар в сайдбаре |
| `cmux clear-progress` | снять прогресс-бар |
| `cmux notify --title --subtitle --body` | синее кольцо на панели + баннер macOS + запись в панели уведомлений |

**Почему ключ `ralphex`, а не `claude_code`:**
`Sources/Workspace+SidebarStatusVisibility.swift:16` — пилюли с ключом **не** из agent-allowlist
показываются безусловно. Ключи из allowlist (`claude_code`, `codex`, …) наоборот скрываются, если
cmux не видит живой PID соответствующего агента. Свой ключ надёжнее и не конфликтует с claude-статусом.

**Почему `workspace loading`, а не `set-status`, двигает лейн воркспейса:**
Лейн (`todo`/`working`/`needs-attention`/`review`/`done`) считается в
`Packages/macOS/CmuxWorkspaces/.../WorkspaceTaskStatus.swift:18` из живых сигналов, а не из пилюль.
`workspace_loading` с ключом `manual:<id>` ставит настоящий `.running` на панель
(`Sources/TerminalController+ControlSidebarContext.swift:191`) — это единственный путь до реального
сигнала, не требующий agent-allowlist или vault-регистрации.

**Ограничение (принято осознанно):** лейн `needs-attention` требует `set_agent_lifecycle` с
зарегистрированным agent-ключом, а CLI режет его собственным хардкод-списком (`CLI/cmux.swift:3020`,
`:25178`) — доступен только через приватный сокет. Вместо него используем `cmux notify`: синее кольцо
на панели, подсветка таба и `Cmd+Shift+U` дают тот же практический UX.

**Валидация id:** `manual:<id>` — 1-64 символа из `[A-Za-z0-9._-]`, не более 32 разных loader-ключей
на воркспейс (`ControlCommandCoordinator+SidebarWorkspaceLoadingV1.swift:18-21`). `ralphex` подходит.

**Точки в коде ralphex:**

- `pkg/status/phase_holder.go:15` — `OnChange` поддерживает ровно один коллбэк; слот уже занят
  веб-дашбордом (`pkg/web/broadcast_logger.go:47`). Нужен список наблюдателей.
- `cmd/ralphex/main.go:579` `executePlan` — единственная воронка для всех режимов исполнения
  (вызовы на строках 395, 811 из `runWithWorktree`, 1525 после создания плана).
- `cmd/ralphex/main.go:1379` `runPlanMode` — отдельный вход для `--plan`, свой `holder` и
  `InputCollector` (строка 1447 `input.NewTerminalCollector`, 1464 `SetInputCollector`).
- `cmd/ralphex/main.go:1687` `startInterruptWatcher` — force-exit через `os.Exit(1)` на строке 1697.
  **Дефёры там не выполняются**, отрабатывает только переданный `cleanup` (ограничен 2 секундами
  через `runCleanupBounded`).
- `pkg/plan/parse.go:31` `Task.Status` (`pending`/`active`/`done`/`failed`) и `ParsePlanFile` — готовый
  источник `done/total` для прогресс-бара, считать ничего не надо.
- `pkg/status/status.go:24-32` — константы фаз (`PhaseTask`, `PhaseReview`, `PhaseExternalReview`,
  `PhaseExternalEval`, `PhasePlan`, `PhaseFinalize`) для маппинга в текст пилюли.
- `pkg/executor/executor.go:264` `claudeChildEnv` — вырезает только `CLAUDECODE` и `ANTHROPIC_API_KEY`,
  все `CMUX_*` наследуются дочерними процессами. Менять не требуется.

**Паттерны проекта, которым следуем:** интерфейсы на стороне потребителя, table-driven тесты с testify,
`t.Helper()` в хелперах, регекспы компилируются на уровне пакета, комментарии строчными буквами,
один `_test.go` на файл исходника.

## Development Approach

- **Testing approach**: обычный (реализация, затем тесты в той же задаче)
- Выполнять каждую задачу полностью перед переходом к следующей
- Небольшие сфокусированные изменения
- **CRITICAL: каждая задача ОБЯЗАНА содержать новые/обновлённые тесты** для своих изменений кода
  - тесты не опциональны, это обязательная часть чеклиста
  - unit-тесты для новых функций/методов
  - unit-тесты для изменённых функций/методов
  - покрытие успешных и ошибочных сценариев
- **CRITICAL: все тесты должны проходить до начала следующей задачи** — без исключений
- **CRITICAL: обновлять этот файл плана при изменении объёма работ**
- Запускать тесты после каждого изменения
- Сохранять обратную совместимость публичных API

## Testing Strategy

- **Unit-тесты**: обязательны для каждой задачи
- **Внешний бинарь `cmux` в тестах не запускается.** `Reporter` вызывает команды через внутренний
  интерфейс-раннер, который в тестах подменяется на фейк, записывающий argv. Тесты проверяют
  сформированный argv, а не поведение cmux.
- **E2E**: у проекта есть playwright e2e для веб-дашборда (`e2e/`). Эта работа UI дашборда не
  затрагивает, новых e2e не требуется. Существующие должны продолжать проходить.
- **Безопасность тестов**: никаких обращений к `~/.config/ralphex/` и к реальному сокету cmux.
  Переменные окружения ставить через `t.Setenv`, файлы — через `t.TempDir()`.

## Progress Tracking

- Отмечать выполненное `[x]` сразу по завершении
- Новые обнаруженные задачи добавлять с префиксом ➕
- Проблемы и блокеры помечать префиксом ⚠️
- Обновлять план, если реализация отклоняется от исходного объёма

## What Goes Where

- **Implementation Steps** (`[ ]`): изменения кода, тесты, документация — всё выполнимое внутри репозитория
- **Post-Completion** (без чекбоксов): ручная проверка в живом cmux, требующая GUI и человека

## Implementation Steps

### Task 1: PhaseHolder поддерживает несколько наблюдателей

Сейчас `OnChange` держит один коллбэк и второй вызов молча затирает первый. Веб-дашборд уже занимает
слот, поэтому cmux-репортер без этой правки работал бы только при выключенном `--serve`.

- [x] в `pkg/status/phase_holder.go` заменить поле `onChange func(old, cur Phase)` на слайс
      `observers []func(old, cur Phase)`
- [x] `OnChange` добавляет наблюдателя в конец списка вместо замены; обновить godoc-комментарий
- [x] `Set` копирует слайс наблюдателей под `RLock` и вызывает их вне блокировки (сохранить текущее
      поведение: коллбэки вызываются только при реальной смене фазы, не под мьютексом)
- [x] nil-коллбэк в `OnChange` игнорируется, а не паникует при `Set`
- [x] написать тесты: два наблюдателя получают событие, порядок вызова совпадает с порядком регистрации
- [x] написать тесты: коллбэк не вызывается при `Set` той же фазы; nil-коллбэк безопасен;
      конкурентные `Set`/`OnChange` под `-race`
- [x] проверить, что `pkg/web` продолжает работать без изменений (существующие тесты пакета зелёные)
- [x] `make test` — должно пройти до начала задачи 2

### Task 2: пакет pkg/cmux — детект окружения и запуск команд

- [x] создать `pkg/cmux/cmux.go` с пакетным godoc-комментарием
- [x] определить неэкспортируемый интерфейс `commandRunner` с методом
      `run(ctx context.Context, args ...string) error` и реализацию поверх `exec.CommandContext`
      (потребительский интерфейс, объявлен здесь же — единственный потребитель)
- [x] `Reporter` с полями: путь к бинарю, `commandRunner`, путь к plan-файлу, таймаут на вызов
      (путь к бинарю живёт в `execRunner` — единственном месте, где он используется; на `Reporter`
      он был бы мёртвым полем)
- [x] `New(planFile string) *Reporter` возвращает `nil`, если `exec.LookPath("cmux")` не нашёл бинарь
      или `CMUX_WORKSPACE_ID` пуст или состоит из пробелов
- [x] все экспортируемые методы nil-safe (`func (r *Reporter) X()` с ранним `if r == nil { return }`),
      чтобы вызывающий код не проверял nil
- [x] внутренний `exec(args ...string)`: короткий таймаут (2 секунды, константа пакета), stdout/stderr
      отбрасываются, ошибка проглатывается — не логировать в прогресс-файл, чтобы не засорять его
- [x] написать тесты: `New` возвращает nil без `CMUX_WORKSPACE_ID` (через `t.Setenv`) и без бинаря в PATH
- [x] написать тесты: все методы на `nil`-приёмнике не паникуют
- [x] написать тесты: ошибка раннера не всплывает наружу; таймаут не подвешивает вызывающего
- [x] `make test` и `make lint` — должны пройти до начала задачи 3 (lint чистый; в `make test` падает
      ⚠️ `cmd/ralphex` `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий
      отказ, воспроизводится и без изменений этого плана, к `pkg/cmux` отношения не имеет)

### Task 3: команды сайдбара и маппинг фаз

- [x] `LoadingOn()` / `LoadingOff()` → argv `{"workspace", "loading", "on"|"off", "--id", "ralphex"}`
- [x] `Status(text, icon, color string)` → `{"set-status", "ralphex", text, "--icon", icon,
      "--color", color, "--priority", "90"}`; пустые icon/color не добавляют своих флагов
- [x] `ClearStatus()` → `{"clear-status", "ralphex"}`
- [x] `Progress(done, total int, label string)` → `{"set-progress", <0.00..1.00>, "--label", label}`;
      `total <= 0` — вызов пропускается, деления на ноль нет; значение форматируется с двумя знаками
      и клампится в `[0, 1]`; ➕ пустой `label` не добавляет своего флага — по аналогии с `Status`
      и `Notify`, чтобы не слать в сайдбар пустую подпись
- [x] `ClearProgress()` → `{"clear-progress"}`
- [x] `Notify(title, subtitle, body string)` → `{"notify", "--title", title, ...}`; пустые subtitle/body
      не добавляют своих флагов; тело обрезается до 200 рун (баннер macOS всё равно не покажет больше)
- [x] `Clear()` — идемпотентно: `LoadingOff` + `ClearStatus` + `ClearProgress`; безопасно вызывать дважды
- [x] добавить маппинг `status.Phase` → (текст пилюли, SF Symbol, hex-цвет) — см. «Technical Details»
- [x] `OnPhase(old, cur status.Phase)` с сигнатурой под `PhaseHolder.OnChange`; неизвестная фаза
      использует своё строковое значение как текст и нейтральную иконку (сюда же попадают легаси-фазы
      `PhaseCodex`/`PhaseClaudeEval` — они только парсятся из старых прогресс-файлов и на живом
      прогоне не выставляются)
- [x] написать тесты: табличные проверки argv для каждого метода, включая пропуск пустых флагов
- [x] написать тесты: `Progress` при `total=0`, при `done > total`, при отрицательных значениях
- [x] написать тесты: маппинг покрывает все константы фаз из `pkg/status`; неизвестная фаза не паникует
- [x] написать тесты: `Clear` идемпотентен и шлёт все три команды
- [x] `make test` и `make lint` — должны пройти до начала задачи 4 (lint чистый, `pkg/cmux` 100%
      покрытия; в `make test` по-прежнему падает ⚠️ `cmd/ralphex`
      `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий отказ,
      перепроверен на чистом дереве через `git stash`, к `pkg/cmux` отношения не имеет)

### Task 4: фоновое обновление прогресса по plan-файлу

Прогресс-бар должен жить во время длинной фазы задач, а не обновляться раз в фазу. Поллинг plan-файла
даёт это, не залезая в `pkg/processor/phase`.

- [x] `Start(ctx context.Context)` — шлёт `LoadingOn`, запускает горутину с `time.Ticker`
      (интервал 10 секунд, константа пакета `pollInterval`; на `Reporter` живёт полем `interval`
      по аналогии с `execTimeout`/`timeout`, чтобы тесты тикали в миллисекундах)
- [x] на каждом тике: `plan.ParsePlanFile`, посчитать задачи со `Status == plan.TaskStatusDone`
      от общего числа, вызвать `Progress(done, total, label)`; лейбл — `<done>/<total> tasks`
- [x] пустой путь к plan-файлу или ошибка парсинга (включая `fs.ErrNotExist` — план мог уехать
      в `completed/`) пропускают тик молча, горутина продолжает работать; ➕ план без секций задач
      (`total == 0`) тоже пропускается, чтобы не запоминать вырожденную пару в `lastDone/lastTotal`
- [x] не слать `Progress`, если пара `done/total` не изменилась с прошлого тика — не дёргать сайдбар зря
      (`lastDone/lastTotal` инициализируются `-1`, чтобы первый тик всегда отчитался; поля трогает
      только горутина поллинга, мьютекс не нужен)
- [x] горутина завершается по `ctx.Done()` или по `Stop()`
- [x] `Stop()` — останавливает горутину, дожидается её выхода, вызывает `Clear()`; защищён `sync.Once`,
      двойной вызов безопасен (важно: `Stop` будет вызываться и из defer, и из interrupt-хендлера);
      ➕ хендлы горутины (`cancel`/`pollDone`) закрыты мьютексом: `sync.Once` защищает от повторного
      выполнения, но не от гонки записи в `Start` с чтением в `Stop` из другой горутины
- [x] написать тесты: подсчёт `done/total` на plan-файлах из `t.TempDir()` (все задачи выполнены,
      частично, ни одной, план без задач)
- [x] написать тесты: отсутствующий/битый plan-файл не роняет горутину
- [x] написать тесты: `Stop` идемпотентен, вызывает `Clear`, завершает горутину (проверка под `-race`)
- [x] написать тесты: отмена контекста останавливает горутину без вызова `Stop`
- [x] `make test` и `make lint` — должны пройти до начала задачи 5 (`pkg/cmux` 100% покрытия, зелёный
      под `-race`; lint чистый по `pkg/cmux`. ⚠️ `golangci-lint` не установлен в PATH, прогонялся
      `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.6.2` — эта версия новее
      закреплённой проектом и даёт 3 предсуществующих замечания `prealloc` в нетронутых файлах
      `pkg/config/defaults.go`, `pkg/git/external.go`, `pkg/notify/notify.go`.
      ⚠️ в `make test` по-прежнему падает `cmd/ralphex`
      `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий отказ,
      перепроверен на чистом дереве через `git stash`, к `pkg/cmux` отношения не имеет)

### Task 5: уведомление когда ralphex ждёт ввода пользователя

`InputCollector` вызывается ровно в те моменты, когда прогон встал и ждёт человека — идеальная точка
для уведомления, не требующая правок в фазах.

- [x] в `pkg/cmux` объявить интерфейс `inputCollector` с методами `AskQuestion` и `AskDraftReview`
      (сигнатуры зеркалят `processor.InputCollector`); ➕ в тестах компайл-тайм проверка
      `var _ inputCollector = processor.InputCollector(nil)` — ловит расхождение сигнатур на сборке,
      не дожидаясь связывания в `cmd/ralphex` (задача 6). Импорт `pkg/processor` только в `_test.go`,
      продакшн-код пакета от него по-прежнему не зависит
- [x] `WrapInput(c inputCollector) inputCollector` возвращает обёртку; при `r == nil` возвращает
      исходный коллектор без изменений
- [x] обёртка перед `AskQuestion` шлёт `Notify("ralphex", "input needed", <текст вопроса>)`
      (⚠️ отклонение от плана: подзаголовок на английском, а не `"нужен ответ"` — остальные
      видимые пользователю строки пакета английские: `task`, `review`, `%d/%d tasks`)
- [x] обёртка перед `AskDraftReview` шлёт `Notify("ralphex", "plan draft ready", <текст вопроса>)`
      (то же отклонение по языку; в теле — параметр `question`, а не первая строка `planContent`:
      это и есть заголовок приглашения к ревью, слать урезанный до 200 рун план бессмысленно)
- [x] после возврата из вложенного коллектора уведомление не шлётся повторно; результат и ошибка
      пробрасываются без изменений
- [x] написать тесты: обёртка вызывает Notify с ожидаемым argv и пробрасывает возвращаемые значения
- [x] написать тесты: обёртка пробрасывает ошибку вложенного коллектора без подмены
      (`assert.Same` — проверяется тождество, а не текст, так что обёртывание ошибки провалит тест)
- [x] написать тесты: `WrapInput` на `nil`-репортере возвращает исходный коллектор
- [x] `make test` и `make lint` — должны пройти до начала задачи 6 (lint чистый по `pkg/cmux`,
      покрытие пакета 100%, зелёный под `-race`. ⚠️ `golangci-lint` не установлен в PATH, прогонялся
      `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.6.2`.
      ⚠️ в `make test` по-прежнему падает `cmd/ralphex`
      `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий отказ,
      к `pkg/cmux` отношения не имеет, файлы `cmd/ralphex` этой задачей не трогались)

### Task 6: включение в main.go

- [x] в `executePlan` (`cmd/ralphex/main.go:579`) создать репортер после `setupProgressLogger`,
      вызвать `Start(ctx)`, поставить `defer rep.Stop()` рядом с `defer plr.closeLog()`
- [x] зарегистрировать `plr.holder.OnChange(rep.OnPhase)` — после создания дашборда, чтобы обе
      подписки сосуществовали
- [x] на завершении прогона слать `Notify` с результатом: успех рядом с `sendNotification(...)` на
      строке 679, провал — в ветке ошибки на строке 664-667; при `ErrUserAborted` уведомление не слать
      (решение об отправке владеет `cmuxCompletionNotice`, а не место вызова)
- [x] в `runPlanMode` (`cmd/ralphex/main.go:1379`) сделать то же самое и дополнительно обернуть
      коллектор: `collector = rep.WrapInput(collector)` перед `r.SetInputCollector`;
      ➕ перед переходом к реализации плана (`runWithWorktree`/`executePlan`) вызывается
      `rep.Stop()`: иначе отложенный `Stop` plan-режима снял бы сайдбар уже начатого прогона
- [x] **добавить `rep.Stop()` в cleanup-коллбэк `startInterruptWatcher`** (`main.go:247`) — на пути
      force-exit через `os.Exit(1)` дефёры не выполняются, и без этого спиннер зависнет в сайдбаре
      навсегда. Репортер должен быть доступен хендлеру так же, как `wtCleanup` (тот же приём с
      синхронизированным холдером, см. `worktreeCleanupFn` на строке 180); ➕ вместо дублирования
      типа `worktreeCleanupFn` переименован в `cleanupHolder` и переиспользован обоими холдерами;
      ➕ сброс сайдбара запускается в отдельной горутине рядом с `wtCleanup.call()`, а не до/после:
      бюджет force-exit общий (2 с), и зависшее удаление воркотри не должно оставлять спиннер висеть
- [x] написать тесты на холдер репортера: потокобезопасная установка и вызов, вызов до установки —
      no-op, двойной вызов безопасен
- [x] написать тесты: сборка уведомления о завершении для успеха, провала и аборта
      (включая завёрнутый `ErrUserAborted` и nil-репортер)
- [x] `make test` и `make lint` — должны пройти до начала задачи 7 (lint чистый по изменённым файлам;
      ⚠️ `golangci-lint` не установлен в PATH, прогонялся
      `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.6.2` — версия новее
      закреплённой проектом, даёт 3 предсуществующих `prealloc` в нетронутых файлах.
      ⚠️ в `make test` по-прежнему падает `cmd/ralphex`
      `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий отказ,
      перепроверен на чистом дереве через `git stash`: тот же `execution context: context canceled`,
      падает до входа в `runPlanMode`)

### Task 7: Verify acceptance criteria

- [x] проверить, что все требования из Overview реализованы — все пять сверены по `cmd/ralphex/main.go`:
      спиннер (`rep.Start` на 638/1479 → `LoadingOn`), пилюля фазы (`holder.OnChange(rep.OnPhase)`
      на 669/1484), прогресс-бар (поллинг плана в горутине `Start`), уведомление на ожидании ввода
      (`rep.WrapInput(collector)` на 1537), уведомление о завершении (`notifyCmuxCompletion`
      на 725/739/1543/1550). Гарантированная очистка на force-exit — `cmuxStop` (`cleanupHolder`)
      и `go cmuxStop.call()` на строке 261
- [x] проверить, что вне cmux (нет бинаря / нет `CMUX_WORKSPACE_ID`) ни одна команда не запускается —
      `New` возвращает `nil` при пустом `CMUX_WORKSPACE_ID` или отсутствии бинаря; все экспортируемые
      методы nil-safe (`exec`, `Start`, `Stop`, `WrapInput` имеют ранний возврат), покрыто тестами
      `TestNewUnsetWorkspaceEnv`, `TestReporterNilReceiver`, `TestReporterWrapInputNilReporter`
- [x] проверить, что `pkg/processor` и `pkg/processor/phase` не изменялись (`git diff --stat`) —
      в диффе `master...HEAD` этих путей нет вовсе
- [x] запустить полный набор тестов: `make test` — все пакеты зелёные, `pkg/cmux` 100% покрытия.
      ⚠️ падает один предсуществующий тест `cmd/ralphex`
      `TestPlanModeIntegration/plan_mode_progress_file_naming`: воспроизведён идентично на `master`
      в отдельном worktree (`"execution context: context canceled" does not contain "plan creation"`),
      к работам этого плана отношения не имеет
- [x] запустить e2e веб-дашборда, если окружение позволяет: `go test -tags=e2e -timeout=10m ./e2e/...`
      — ⚠️ падает один предсуществующий тест `TestPhaseNavigation/all_tabs_visible`: ожидает легаси-вкладки
      `Claude Review` / `Codex Review`, переименованные работой symmetric-cross-model-external-review.
      Воспроизведён идентично на `master`. UI дашборда этим планом не затрагивался
- [x] запустить линтер: `make lint` — по изменённым файлам чисто. ⚠️ `golangci-lint` не установлен
      в PATH, прогонялся `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.6.2`
      (проект версию не пиннит, CI берёт `golangci-lint-action@v9`). Остаются 3 замечания `prealloc`
      в `pkg/config/defaults.go`, `pkg/git/external.go`, `pkg/notify/notify.go` — все три файла
      не менялись ни этим планом, ни веткой целиком (`git log master..HEAD -- <files>` пуст),
      правка нарушила бы правило «no general improvements to unrelated code»
- [x] проверить сборку под Windows: `GOOS=windows GOARCH=amd64 go build ./...` (пакет не должен
      использовать unix-специфичные вызовы) — exit 0
- [x] проверить покрытие нового пакета — не ниже 80% — `pkg/cmux` 100.0%, зелёный под `-race`
      (вместе с `pkg/status`)
- [x] сверить контрольные суммы `~/.config/ralphex/` до и после прогона тестов — конфиг не тронут:
      19 файлов, `diff` до/после пуст

### Task 8: [Final] Update documentation

- [x] добавить раздел про интеграцию с cmux в `CLAUDE.md` (что делает, где точки включения,
      почему очистка обязана висеть на interrupt-хендлере) — новый раздел «cmux Sidebar Integration»
      после «Plan Creation Mode»: авто-детект и nil-safety, best-effort с проглатыванием ошибок,
      неприкосновенность `pkg/processor`, обоснование ключа `ralphex` и `workspace loading`,
      поллинг plan-файла, обязательная очистка через `cmuxStop`/`cleanupHolder` на force-exit,
      точки включения в `executePlan`/`runPlanMode` и `notifyCmuxCompletion`, список ключевых файлов;
      ➕ дополнительно бullet в «Key Patterns» про `PhaseHolder.OnChange` со списком наблюдателей —
      контракт общего типа, его читают при добавлении новых подписчиков фаз, а не в разделе про cmux
- [x] добавить пользовательское описание в `llms.txt`: что появляется в сайдбаре, что интеграция
      включается автоматически и требует только запуска ralphex из терминала cmux — блок
      «cmux sidebar integration» рядом с «Notifications»
- [x] дописать `pkg/cmux/` в дерево «Project Structure» в `CLAUDE.md`
- [x] `make test` — задача меняет только markdown (`git diff --stat`: `CLAUDE.md`, `llms.txt`),
      Go-файлы не тронуты, поэтому `make lint` не затрагивается. ⚠️ в `make test` по-прежнему падает
      `cmd/ralphex` `TestPlanModeIntegration/plan_mode_progress_file_naming` — предсуществующий
      отказ, воспроизведён на чистом дереве в задачах 2-7, к документации отношения не имеет

## Technical Details

### Маппинг фаз в пилюлю статуса

Иконки — SF Symbols (cmux рендерит их нативно), цвета — hex. Значения ниже утверждены как есть:
цель первой итерации — работающий PoC, подгонка текста/иконок/цветов под конкретный сайдбар идёт
отдельным проходом после проверки в живом cmux.

| `status.Phase` | Текст пилюли | Иконка | Цвет |
|---|---|---|---|
| `PhaseTask` | `task` | `hammer` | `#22c55e` |
| `PhaseReview` | `review` | `magnifyingglass` | `#06b6d4` |
| `PhaseExternalReview` | `external review` | `person.2` | `#a855f7` |
| `PhaseExternalEval` | `evaluating findings` | `checkmark.seal` | `#a855f7` |
| `PhaseFinalize` | `finalize` | `flag.checkered` | `#22c55e` |
| `PhasePlan` | `planning` | `list.bullet.clipboard` | `#3b82f6` |
| неизвестная | значение фазы как есть | `circle` | пусто |

### Форма вызовов

Аргументы передаются как отдельные элементы argv (`exec.CommandContext`), не как строка — экранирование
не требуется, текст с пробелами и юникодом проходит как есть. Целевой воркспейс не указывается: cmux CLI
по умолчанию берёт `$CMUX_WORKSPACE_ID`, который cmux инжектит в каждый свой терминал и который
наследуется дочерними процессами.

### Интервалы и таймауты

| Параметр | Значение | Обоснование |
|---|---|---|
| Таймаут одного вызова `cmux` | 2 с | сокет локальный; висящий вызов не должен тормозить прогон |
| Интервал поллинга плана | 10 с | итерация задачи идёт минуты, чаще незачем |
| Обрезка тела уведомления | 200 рун | баннер macOS больше не покажет |

### Что осознанно не делается

- **Лейн `needs-attention`.** Требует приватного сокета cmux; `cmux notify` даёт эквивалентный UX
  (синее кольцо, подсветка таба, `Cmd+Shift+U`).
- **`cmux todo set`** — зеркалирование чекбоксов плана в чеклист воркспейса. `docs/cli-contract.md:475`
  прямо просит агентов не трогать чеклист без явной просьбы пользователя. Прогресс-бар закрывает ту же
  потребность без вторжения.
- **Конфиг-флаг включения.** Авто-детект по окружению; отдельная настройка не нужна.
- **Логирование ошибок вызовов `cmux`.** Индикация не должна засорять прогресс-файл.

## Post-Completion

*Требует человека и GUI, автоматизации не поддаётся*

**Ручная проверка в живом cmux:**
- Запустить ralphex в терминале cmux, убедиться: появился спиннер, пилюля меняется на переходах фаз,
  прогресс-бар растёт по мере выполнения задач
- Проверить, что после нормального завершения спиннер и прогресс-бар сняты, пришло уведомление
- Проверить очистку по Ctrl+C — как при одиночном (graceful), так и при двойном (force-exit через 5 с)
- Проверить `--plan`: приходит уведомление на вопросе и на готовом черновике
- Проверить одновременный запуск двух ralphex в разных воркспейсах — статусы не путаются между собой
- Проверить работу с `--serve`: и дашборд, и сайдбар cmux получают события фаз
